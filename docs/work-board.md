# BrewMap Work Board

작성일: 2026-06-29
갱신일: 2026-07-07

이 문서는 주차별 진행 상태의 단일 소스다. `docs/mvp-plan.md`, `docs/week5-login-save-plan.md`, `docs/admin-content-management-plan.md`의 상태 표는 이 문서로 위임하며, 각 문서에는 계획·완료 기준·QA 근거만 남긴다.

## 진행 기준

- 착수일은 Git 최초 커밋일인 2026-06-15로 본다.
- 2026-07-07 현재 착수 23일차이며, Week 5(로그인/저장) 자동 QA는 완료됐다. 실메일 새로고침 세션 유지, Google OAuth 실복귀, production DELETE 401은 운영자 확인 항목으로 남았다.
- 초기 카페 수 150개 이상은 차후에도 지속 추가 가능한 항목이므로 현재 마일스톤 판단에서는 완료로 본다.
- 로그인 기능은 Google OAuth와 이메일 링크 기준으로 동작한다. 공개 랜딩은 검색/지도 중심으로 유지하고 저장/제보는 로그인 후 작업 공간에서 표시한다.

## 목표와 일정

| 구분 | 목표 | 상태 | 일정 기준 |
| --- | --- | --- | --- |
| 현재 | 작업 보드 완료 | Done | 2026-06-29 완료 |
| 완료 | Week 4 작업 목록 완료 | Done | 2026-06-29 검증 완료 |
| 완료 | 로그인 후 저장/제보 작업 공간 분리 | Done | 2026-07-07 자동 QA 완료, 실계정 OAuth는 운영자 확인 |

## 현재 우선순위

| 순서 | 목표 | 완료 기준 | 상태 |
| --- | --- | --- | --- |
| 1 | 작업 보드 완료 | MVP 완료 기준, 주차별 목표, 다음 작업 순서가 문서화되어 있다. | Done |
| 2 | Week 4 작업 목록 완료 | 필터, 상세, 외부 지도, 필터 QA, 샘플 100개 기준을 검증한다. | Done |
| 3 | 로그인/저장 계획 수립 및 1차 구현 | 로그인, 저장/해제, 저장 리스트, 저장 QA 작업을 구현 가능한 단위로 쪼개고 저장 API부터 착수한다. | Done |
| 4 | 로그인 후 저장/제보 작업 공간 분리 | 공개 랜딩은 검색/지도 중심으로 유지하고, 저장/제보 메뉴와 카드는 로그인 후 표시한다. | Done (2026-07-07 자동 QA) |
| 5 | Admin 페이지/콘텐츠 관리 계획 | 공개 홈, 공지, 큐레이션, 안내 콘텐츠를 Admin에서 관리하는 Week 6 작업 계획을 확정한다. | Done (2026-07-06, `docs/admin-content-management-plan.md`) |

## 작업 보드 완료 체크리스트

| 항목 | 완료 기준 | 상태 |
| --- | --- | --- |
| MVP 기준 재정의 | 초기 카페 수 150개 이상은 현재 마일스톤에서 완료로 표시하고, 데이터 추가는 지속 운영 항목으로 분리한다. | Done |
| 주차별 목표 정렬 | Week 1~3 완료 상태와 Week 4 이후 목표가 분리되어 있다. | Done |
| 우선순위 고정 | 작업 보드 완료, Week 4, 로그인/저장 순서가 명시되어 있다. | Done |
| 검증 명령 고정 | `npm run lint`, `npm run data:check`, `npm run build`를 기본 확인 명령으로 둔다. | Done |
| README 반영 | 최신 구성, 목표, 일정, 주요 문서 링크가 README에 정리되어 있다. | Done |
| MVP 계획 반영 | `docs/mvp-plan.md`에 2026-06-29 운영 업데이트가 추가되어 있다. | Done |

## Week 4 작업 목록

| 작업 | 완료 기준 | 상태 |
| --- | --- | --- |
| 필터 동작 검증 | 핵심 커피 필터 버튼이 검색 결과와 지도 상태를 함께 갱신한다. | Done |
| 상세 화면 검증 | 카페 상세에서 가능한 커피, 검증 상태, 최근 확인일, 저장 상태를 확인할 수 있다. | Done |
| 외부 지도 링크 검증 | 네이버지도, 카카오맵, Google Maps 링크아웃이 각 카페 기준으로 동작한다. | Done |
| 필터 QA | 필터별 결과 없음, 복수 필터, 검색어와 필터 조합 상태를 확인한다. | Done |
| 샘플 100개 기준 | Seed CSV가 100개 이상이고 구조 검증을 통과한다. | Done |

## Week 4 검증 명령

```bash
npm run lint
npm run data:check
npm run build
```

브라우저 검증 산출물: `artifacts/week4-browser-validation.json`, `artifacts/week4-validation-desktop.png`. 필터 적용 결과는 113개에서 24개로 줄었고, 지도 마커 18개, 상세 모달, 외부 지도 링크, 빈 결과 상태가 통과했다.

## 다음 목표: 로그인/저장

Week 4 작업이 완료되었으므로 `docs/week5-login-save-plan.md` 기준으로 Week 5 작업에 착수한다.

| 작업 | 완료 기준 | 상태 |
| --- | --- | --- |
| 인증 방식 결정 | Supabase Auth, 자체 세션, 임시 이메일 링크 등 MVP용 인증 방식을 하나로 고정한다. | Done |
| 저장 데이터 모델 확정 | 로그인 사용자 기준 저장 리스트와 저장 카페 관계를 확정한다. | Done |
| 저장 API 구현 | 인증 사용자가 저장 목록 조회, 저장, 해제를 할 수 있다. | Done |
| 인증 상태/이메일 로그인 구현 | 저장 패널에서 이메일 로그인 링크를 요청하고 인증 상태를 표시한다. | Done |
| 로그인 콜백/임시 저장 병합 | 이메일 링크 콜백 토큰을 저장하고 기존 임시 저장을 계정 저장 목록에 병합한다. | Done |
| 저장/해제 서버 동기화 | 로그인 사용자가 카페를 저장하고 해제할 때 서버 저장 목록과 동기화한다. | Done |
| 저장 리스트 구현 | 로그인 사용자가 저장한 카페 목록을 다시 볼 수 있다. | Done |
| 저장 QA | 로그인 전/후, 새로고침, 다른 기기 접근, 로그아웃 상태를 확인한다. | Done (2026-07-07 자동 QA, 실메일 새로고침은 운영자 확인) |
| 실메일 링크 QA | 실제 이메일 링크를 받고 콜백 토큰으로 서버 저장/병합을 확인한다. | Done |

상태 근거: `docs/week5-login-save-plan.md`의 2026-07-02 Production Live QA와 2026-07-07 자동 QA. 인증 저장/해제 서버 반영, 로그아웃 fallback, 실계정 저장 목록 동기화, mock refresh token 세션 복구까지 확인했다. 실메일 새로고침 세션 유지와 Google OAuth 실복귀는 운영자 확인 항목이다.

## 다음 목표: 로그인 후 작업 공간 분리

저장/제보는 공개 랜딩의 보조 카드가 아니라 로그인 사용자의 작업 공간으로 분리한다. 2026-07-07 자동 QA에서 비로그인/로그인 메뉴, pending 저장/제보 복귀, localStorage refresh, refresh token 상태 전환을 확인했다.

| 작업 | 완료 기준 | 상태 |
| --- | --- | --- |
| 로그인 전용 화면 | Google, Apple, 이메일 로그인 진입점을 한 화면에 모은다. | Done |
| 공개 랜딩 단순화 | 비로그인 상단 메뉴는 발견, 결과, 지도, 로그인 중심으로 표시한다. | Done |
| 저장/제보 인증 조건 | 저장한 카페 카드와 정보 제보 카드는 로그인 후 표시한다. | Done |
| 비로그인 액션 유도 | 비로그인 사용자가 저장/제보를 누르면 로그인 후 원래 작업으로 돌아온다. | Done |
| QA | 로그인 전/후 메뉴, 저장/제보 표시, OAuth 복귀, 저장 병합을 확인한다. | Done (자동 QA, Google OAuth 실복귀는 운영자 확인) |

## 다음 목표: Admin 페이지/콘텐츠 관리

로그인 후 작업 공간 분리 자동 QA가 끝났으므로 `docs/admin-content-management-plan.md` 기준으로 Week 6 Admin 작업을 이어간다. 기존 Admin의 카페/태그/제보/CSV 운영 기능은 유지하고, 공개 페이지와 콘텐츠를 관리하는 메뉴와 화면을 추가한다.

| 작업 | 완료 기준 | 상태 |
| --- | --- | --- |
| 메뉴/화면 설계 | Admin 메뉴에 콘텐츠 관리 진입점, 대시보드, 페이지 목록, 페이지 편집, 블록 편집, 미리보기를 정의한다. | Done (2026-07-06 계획 확정) |
| 콘텐츠 스키마 | `site_pages`, `content_blocks`, `content_revisions`와 RLS 정책 초안을 만든다. | Done (마이그레이션 커밋, Supabase 적용은 운영자 확인) |
| 공개 콘텐츠 API | published 페이지와 visible 블록만 반환하는 `/api/content?slug=home`을 추가한다. | Done |
| Admin 콘텐츠 API | 페이지/블록 저장, 게시, 보관 작업을 관리자 권한으로 처리한다. | Done (실DB 쓰기 검증은 운영자 확인) |
| Admin UI 구현 | `/admin`에서 콘텐츠 관리 메뉴와 편집 화면을 사용할 수 있다. | Done |
| 공개 홈 연동 | 홈 히어로, 공지, 큐레이션 카드가 콘텐츠 API를 사용하고 실패 시 기존 정적 콘텐츠로 fallback한다. | Done |
| 제보 E2E 검증 | 비로그인 제보 게이트, 로그인 후 초안 복원, Admin 승인/반려, 운영 로그 기록을 확인한다. | Done (2026-07-07 자동 QA, 실DB 반영은 운영자 확인) |
| QA | Admin API 인증 경계, published-only 공개 API, fallback, lint/data:check/build를 확인한다. | Done (자동 검증, Supabase 실DB 게시 흐름은 운영자 확인) |

2026-07-07 기준 콘텐츠 관리 스키마/API/Admin UI와 공개 홈 연동은 코드 기준 완료됐다. 제보 E2E 자동 QA는 비로그인 제보 제출 게이트, 모의 세션 로그인 후 `#report` 복귀와 초안 복원, 빈 제보 입력 안내, `/admin` 검토 대기열 표시, 승인 시 폐업 상태 반영, 반려 사유 저장, `admin_logs` 기록을 통과했다. 산출물은 `artifacts/report-e2e/admin-queue.png`, `artifacts/report-e2e/approval-rejection-result.png`에 보관한다. Supabase 마이그레이션 적용, 실DB draft 저장/게시/보관 확인, production DELETE성 작업, 제보 승인 결과의 실DB 반영 확인은 운영자 확인 항목이다.

## 지속 운영 항목

| 항목 | 기준 |
| --- | --- |
| 카페 데이터 추가 | 150개 이상 목표는 현재 마일스톤에서 완료로 보되, 베타와 퍼블릭 MVP 전까지 계속 보강한다. |
| 평균 태그 수 개선 | 카페당 평균 커피 태그 3개 이상을 목표로 계속 보강한다. |
| 필터 커버리지 개선 | `decaf`, `cold_brew`, `flat_white` 샘플을 추가해 MVP 필터 커버리지를 높인다. |
